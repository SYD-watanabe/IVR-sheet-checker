# Git Bash コマンド完全ガイド
# IVR-sheet-checker をGitHubにプッシュする手順

## ========================================
## ステップ1: プロジェクトフォルダに移動
## ========================================

# プロジェクトのフォルダに移動（パスは環境に合わせて変更してください）
cd /c/Users/YourName/Projects/IVR-sheet-checker

# または、デスクトップにある場合
cd ~/Desktop/IVR-sheet-checker

# 現在のディレクトリを確認
pwd


## ========================================
## ステップ2: 現在の状態を確認
## ========================================

# 変更されたファイルを確認
git status

# 出力例:
# On branch main
# Changes not staged for commit:
#   modified:   index.html
#   modified:   README.md
#
# Untracked files:
#   CHANGELOG.md
#   push-to-github.sh
#   push-to-github.bat


## ========================================
## ステップ3: 変更内容を確認（オプション）
## ========================================

# 変更の差分を確認（詳細を見たい場合）
git diff index.html

# 新規ファイルを確認
ls -la

# 特定のファイルの状態を確認
git status README.md


## ========================================
## ステップ4: すべての変更をステージングエリアに追加
## ========================================

# すべての変更と新規ファイルを追加
git add .

# または、個別にファイルを追加する場合
# git add index.html
# git add README.md
# git add CHANGELOG.md
# git add push-to-github.sh
# git add push-to-github.bat

# ステージングされた内容を確認
git status


## ========================================
## ステップ5: コミットする
## ========================================

# 方法A: 簡単なコミットメッセージ（1行）
git commit -m "feat: バッチ処理改善と修正済み文章コピー機能追加 (v1.1.0)"


# 方法B: 詳細なコミットメッセージ（複数行・推奨）
git commit -m "feat: バッチ処理改善と修正済み文章コピー機能追加" \
-m "" \
-m "主な変更:" \
-m "- バッチサイズを10に増量（RPM削減: 1/10）" \
-m "- バッチ間に3秒待機（レート制限対策強化）" \
-m "- 修正済み文章の自動生成機能を追加" \
-m "- ワンクリックコピー機能を実装" \
-m "- より詳細なエラーメッセージ表示" \
-m "- 進捗表示の改善（バッチ番号/総数）" \
-m "" \
-m "技術的改善:" \
-m "- copyToClipboard() 関数の追加" \
-m "- escapeRegExp() 関数の追加" \
-m "- displayErrorResults() の UI 改善" \
-m "- レート制限エラーハンドリングの強化" \
-m "" \
-m "ユーザーメリット:" \
-m "- 処理速度の向上（約8秒/10オブジェクト）" \
-m "- 200個のオブジェクトまで確実に処理可能" \
-m "- 作業効率が10倍向上（手動修正不要）" \
-m "- コピー&ペーストで即座に修正可能" \
-m "" \
-m "Version: 1.1.0"


# コミットが成功したか確認
git log --oneline -1


## ========================================
## ステップ6: GitHubにプッシュ
## ========================================

# リモートリポジトリの確認
git remote -v

# 出力例:
# origin  https://github.com/YOUR_USERNAME/IVR-sheet-checker.git (fetch)
# origin  https://github.com/YOUR_USERNAME/IVR-sheet-checker.git (push)


# mainブランチにプッシュ
git push origin main

# または、シンプルに
git push


# プッシュ中の出力例:
# Enumerating objects: 12, done.
# Counting objects: 100% (12/12), done.
# Delta compression using up to 8 threads
# Compressing objects: 100% (8/8), done.
# Writing objects: 100% (8/8), 15.23 KiB | 3.81 MiB/s, done.
# Total 8 (delta 4), reused 0 (delta 0), pack-reused 0
# To https://github.com/YOUR_USERNAME/IVR-sheet-checker.git
#    a1b2c3d..e4f5g6h  main -> main


## ========================================
## ステップ7: プッシュ成功を確認
## ========================================

# 最新のコミットを確認
git log --oneline -3

# リモートとの同期状態を確認
git status

# 出力例:
# On branch main
# Your branch is up to date with 'origin/main'.
# nothing to commit, working tree clean


## ========================================
## トラブルシューティング
## ========================================

# === エラー1: リモートリポジトリが設定されていない ===
# エラーメッセージ: fatal: No configured push destination.

# 解決策: リモートリポジトリを追加
git remote add origin https://github.com/YOUR_USERNAME/IVR-sheet-checker.git

# 確認
git remote -v


# === エラー2: 認証エラー ===
# エラーメッセージ: Authentication failed

# 解決策A: Personal Access Token (PAT) を使用
# 1. GitHub Settings → Developer settings → Personal access tokens
# 2. "Generate new token" → "repo" スコープを選択
# 3. トークンをコピー
# 4. プッシュ時にユーザー名とトークンを入力

# 解決策B: SSH鍵を使用
# リモートURLをSSHに変更
git remote set-url origin git@github.com:YOUR_USERNAME/IVR-sheet-checker.git


# === エラー3: ブランチが遅れている ===
# エラーメッセージ: Updates were rejected because the tip of your current branch is behind

# 解決策: まずpullしてからpush
git pull origin main --rebase
git push origin main


# === エラー4: コンフリクト ===
# エラーメッセージ: CONFLICT (content): Merge conflict

# 解決策: コンフリクトを解決
git status  # コンフリクトしているファイルを確認
# ファイルを手動編集してコンフリクトを解決
git add .
git commit -m "fix: resolve merge conflict"
git push


## ========================================
## 便利なコマンド
## ========================================

# コミット履歴を見やすく表示
git log --graph --oneline --all --decorate

# 特定のファイルの変更履歴
git log --follow -- index.html

# 最後のコミットを修正（まだpushしていない場合）
git commit --amend -m "新しいメッセージ"

# 最後のコミットを取り消し（変更は保持）
git reset --soft HEAD~1

# 最後のコミットを完全に取り消し（変更も削除）
git reset --hard HEAD~1

# 特定のファイルをステージングから外す
git restore --staged index.html


## ========================================
## プッシュ後の確認
## ========================================

# ブラウザでGitHubリポジトリを開く（Windowsの場合）
start https://github.com/YOUR_USERNAME/IVR-sheet-checker

# Mac/Linuxの場合
open https://github.com/YOUR_USERNAME/IVR-sheet-checker
# または
xdg-open https://github.com/YOUR_USERNAME/IVR-sheet-checker


## ========================================
## 完了！
## ========================================

echo "✅ GitHubへのプッシュが完了しました！"
echo "🌐 リポジトリURL: https://github.com/YOUR_USERNAME/IVR-sheet-checker"
echo "📝 次のステップ:"
echo "  1. GitHubでコミットを確認"
echo "  2. (オプション) リリースタグv1.1.0を作成"
echo "  3. (オプション) GitHub Pagesを確認"
